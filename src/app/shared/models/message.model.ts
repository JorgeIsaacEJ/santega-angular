export interface MessageModels {
    mail: string;
    typeMail: string;
    codigo: string
}
export interface MailModels {
    nombre: string;
    correo: string;
    telefono: string;
    mensaje: string
}
export interface SMSModels {
    sms: string;
    telefono: string;
}